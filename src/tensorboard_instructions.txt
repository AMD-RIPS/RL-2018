To use tensorboard just open a terminal in a folder that is a parent folder of the log file you want to open. Then run
>>> tensorboard --logdir .
If it tells you the port is being  used then run
>>> pkill tensorboard
>>> tensorboard --logdir .

